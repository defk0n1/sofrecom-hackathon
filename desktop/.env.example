# Desktop Application Environment Variables

# This file contains environment variable templates for the desktop application
# Copy the relevant sections to their respective .env files

# ============================================
# Backend Configuration (backend/.env)
# ============================================
# Required: Get your API key from https://makersuite.google.com/app/apikey
GEMINI_API_KEY=your_gemini_api_key_here

# Server Configuration
HOST=0.0.0.0
PORT=5000
DEBUG=True

# CORS Configuration
# In desktop mode, the frontend runs on port 8080
ALLOWED_ORIGINS=http://localhost:8080,http://localhost:5173

# Optional: Custom Tesseract path (if not in system PATH)
# TESSERACT_CMD=/usr/bin/tesseract

# ============================================
# Frontend Configuration (MailMate-AI/.env)
# ============================================
# API URL for the backend
# In desktop mode, backend always runs on localhost:5000
VITE_API_URL=http://localhost:5000

# ============================================
# Desktop Configuration (desktop/.env)
# ============================================
# Optional: Electron specific settings
# DEV_TOOLS=true  # Open DevTools on startup in dev mode
# START_MINIMIZED=false
# WINDOW_WIDTH=1400
# WINDOW_HEIGHT=900

# ============================================
# Gmail API Configuration (if using Gmail features)
# ============================================
# See backend/credentials.json.example for Gmail OAuth setup
# GOOGLE_CLIENT_ID=your_client_id_here
# GOOGLE_CLIENT_SECRET=your_client_secret_here

# ============================================
# Notes
# ============================================
# 1. Never commit .env files with real credentials
# 2. For production builds, sensitive keys should be configured post-install
# 3. The desktop app will use backend/.env when starting the backend process
# 4. Frontend .env is bundled during build, so don't put secrets there
